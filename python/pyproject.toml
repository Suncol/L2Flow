[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "l2flow-realtime"
version = "2.0.0"
description = "Observed-universe read-only client for L2Flow Wire V2"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["l2flow_realtime"]
